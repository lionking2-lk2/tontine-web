function Navbar(){

    return (
        <nav>
            <h2>Tontine</h2>

            <div>
                <a href="/">Accueil</a>
                <a href="/login">Connexion</a>
            </div>
        </nav>
    );

}

export default Navbar;
